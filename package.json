{
  "name": "shawarma-time-showcase",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"console.log('Static Netlify build: no compilation required')\"",
    "admin:create": "node scripts/create-first-admin.mjs",
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.87.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
